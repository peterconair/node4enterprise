#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../orientjs/bin/orientjs" "$@"
  ret=$?
else 
  node  "$basedir/../orientjs/bin/orientjs" "$@"
  ret=$?
fi
exit $ret
